<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_zlock_angle_list"
        android:entryValues="@array/pref_zlock_angle_list_values"
        android:key="example_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_zlock_angle" />

    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_trip_angle_list"
        android:entryValues="@array/pref_trip_angle_list_values"
        android:key="example_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_trip_angle" />

</PreferenceScreen>
